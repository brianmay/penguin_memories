name: Build and Deploy to Docker Hub

on:
  - push
  - pull_request

jobs:
  test:
    name: Setup, Build, Test
    uses: brianmay/workflows/.github/workflows/elixir.yaml@c66c260e166cbecc318c9105d970e56cb151c7e8
    with:
      apt_packages: "libimage-exiftool-perl ffmpeg exiftran dcraw"
      test_data: "https://www.dropbox.com/s/yg6jdk9lfewn8yf/penguin_memories.tar?dl=0"

  setup-build-publish-deploy:
    name: Setup, Build, Publish, and Deploy
    uses: brianmay/workflows/.github/workflows/docker.yaml@c66c260e166cbecc318c9105d970e56cb151c7e8
    needs:
      - test
